/*import crypto from "crypto";

export async function sendRestoreLink(req, res) {
  const token = crypto.randomBytes(32).toString("hex"); // 🔒 token único
  // Guarda el token en la BD asociado al usuario y con fecha de expiración
  // Luego envía el correo:
  const restoreURL = `http://tu-dominio/admin/restore/${token}`;
  // enviar restoreURL por email
}*/
